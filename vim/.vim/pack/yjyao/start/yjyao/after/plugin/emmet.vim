" Don't automatically activate emmet.
" To list all filetypes with emmet enabled, run
"     $ grep -lr EmmetInstall ~/.vim/ | grep -v /pack/
let g:user_emmet_install_global = 0

let g:user_emmet_leader_key = '<C-e>'
